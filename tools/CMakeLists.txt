# ---------------------------------------------------------------------------
# Developer tools
#
# The tools are only built when the project is configured with -DBUILD_TOOLS=ON
# (see the root CMakeLists.txt).  Each tool lives in its own directory.
# ---------------------------------------------------------------------------
add_subdirectory(svg2png)
